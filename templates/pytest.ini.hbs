{{!-- templates/pytest.ini.hbs — pytest config for Python profiles (C3) --}}
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = --cov --cov-fail-under={{answers.testing.coverage_threshold}} --cov-report=term-missing
